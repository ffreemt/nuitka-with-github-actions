name: my workflow
on: push

jobs:
  job1:
    runs-on: windows-latest
    defaults:
      run:
        # shell: pwsh
        shell: cmd
    steps:
      - name: Hello world
        run: |
          # write-output "Hello World"
          echo "Hello World"
